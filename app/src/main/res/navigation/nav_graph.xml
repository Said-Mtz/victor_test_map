<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/fragmentHome">

    <fragment
        android:id="@+id/fragmentHome"
        android:name="com.example.examenfintecimal.ui.fragments.home.FragmentHome"
        android:label="fragment_home"
        tools:layout="@layout/fragment_home">
        <action
            android:id="@+id/action_fragmentHome_to_fragmentMap"
            app:destination="@id/fragmentMap" />
    </fragment>

    <fragment
        android:id="@+id/fragmentMap"
        android:name="com.example.examenfintecimal.ui.fragments.map.FragmentMap"
        android:label="fragment_map"
        tools:layout="@layout/fragment_map" />

</navigation>